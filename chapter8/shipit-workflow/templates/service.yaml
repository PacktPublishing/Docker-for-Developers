apiVersion: v1
kind: Service
metadata:
  name: {{ include "shipit-workflow.fullname" . }}
  labels:
    {{- include "shipit-workflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shipit-workflow.selectorLabels" . | nindent 4 }}
