apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shipit-workflow.fullname" . }}-test-connection"
  labels:
    {{- include "shipit-workflow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shipit-workflow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
